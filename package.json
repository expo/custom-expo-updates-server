{
  "name": "custom-expo-updates-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "test": "jest",
    "expo-publish": "cd client && expo export --experimental-bundle --force && cd ../ && rm -rf updates/1/ && cp -r client/dist/ updates/1"
  },
  "dependencies": {
    "mime": "^2.5.2",
    "next": "10.1.3",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "devDependencies": {
    "babel-jest": "^26.6.3",
    "jest": "^26.6.3",
    "node-mocks-http": "^1.10.1"
  }
}
